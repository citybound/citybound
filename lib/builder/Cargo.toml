[package]
name = "builder"
version = "0.1.0"
authors = ["The Citybound Developers"]

[lib]
crate-type = ["dylib"]

[dependencies]
clippy = "0.0.106"
cargo = "0.15.0"
uuid = { version = "0.3", features = ["v4"] }
toml = "0.2"

weaver = { path = "../../lib/weaver" }
